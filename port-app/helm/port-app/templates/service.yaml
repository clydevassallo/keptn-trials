apiVersion: v1
kind: Service
metadata:
  name: {{ include "port-app.name" . }}
  labels:
    {{- include "port-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "port-app.selectorLabels" . | nindent 4 }}
